<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_area :address %>
  </div>
  <div class="field">
  	 <%= f.label :languages %><br />
  	 <%= language_autocomplete_js 'user_languages' %>
  	 <% tmp = Array.new %>
      <% @user.languages.each{|lang| %>
        <% tmp << lang.english_name %>
      <%}%>
  	 <%= f.text_field :languages, :value => tmp.join(",") %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
